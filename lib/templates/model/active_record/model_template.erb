require 'rails_helper'

RSpec.describe <%= @model.model %>, regressor: true do

  <%= '# === Relations ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.belong_to_relations %>
  <%= @model.has_one_relations %>
  <%= @model.has_many_relations %>

  <%= '# === Nested Attributes ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.nested_attributes %>

  <%= '# === Database (Columns) ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.columns %>

  <%= '# === Database (Indexes) ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.indices %>

  <%= '# === Validations (Length) ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.length_validators %>

  <%= '# === Validations (Presence) ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.presence_validators %>

  <%= '# === Validations (Numericality) ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.numericality_validators %>

  <% if Regressor.configuration.include_enums %>
  <%= '# === Enums ===' unless Regressor.configuration.disable_comment_headers %>
  <%= @model.enums %>
  <% end %>
end